# Research Wiki — Python 파이프라인 의존성 (GitHub Actions 실행)
# Session 0 스켈레톤: 스크래퍼 이월분 기준. Phase 1~2에서 확장.

# ── 스크래퍼 (equity-research-blog 이월) ──
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
python-dateutil>=2.9.0
feedparser>=6.0.11

# ── Neon 직접 접근 (raw SQL) · 설정 로드 ──
psycopg[binary]>=3.2.0
PyYAML>=6.0.2

# ── LLM (OpenAI 단일 공급자) ──
# 필터/스코어링·추출·요약·wiki·임베딩·PDF 폴백 전부 OpenAI 사용.
# Responses API PDF file_data 입력(pdf/parse.py)에 최신 SDK 필요.
openai>=1.66.0

# ── Phase 1 추가 ──
docling>=2.0.0            # PDF 파싱 (표 보존, D6)
# crawl4ai>=0.4.0         # Tier 3 폴백 (선택 설치)

# ── 테스트 ──
pytest>=8.0.0
